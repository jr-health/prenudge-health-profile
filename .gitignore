# IDE
.idea/

# Claude Code memory
.claude/

# Python
__pycache__/
*.pyc

# Local clone of the sveltia-cms-auth worker source (deployed separately via wrangler)
sveltia-cms-auth/

# Local preview output of scripts/render_index.py (only ever built into _site/ in CI,
# never committed to the repo root) - see doc/interactive-catalogue-plan.md
/index.html
/index.de.html
/downloads.en.html
/downloads.de.html
/_site/
/_site_test/

# Versioned reports/exports: these are GitHub Release assets, never repo content
# (see README "Repository Layout"). They land in the repo root when the release
# chain is run locally, and in docx-preview/ via the local Word-export preview.
/health-profile-v*.md
/health-profile-v*.adoc
/health-profile-v*.docx
/docx-preview/
